[package]
name = "looper-git"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Git repository discovery & tracking via pure-Rust gix (history/blame later)."

[dependencies]
gix = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
