# Frontend — Architecture

Vite + React + TypeScript, feature-folder layout with colocated styles. A thin data layer wraps the
backend API; server state lives in a small query cache, UI state stays local.
